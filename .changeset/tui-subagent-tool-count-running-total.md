---
"@moonshot-ai/kimi-code": patch
---

Show subagent tool counts as `running/total` (e.g. `· 3/12 tools`) in the agent-group card and the single subagent header, so an in-flight sub-tool call is no longer hidden behind the cumulative count.